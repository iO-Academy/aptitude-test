export interface Scores {
    percentage: number|string,
    topGrade?: boolean,
    passingGrade?: boolean,
    fail?: boolean,
    notTakenYet?: boolean,
    autoCompleted?: boolean,
    results?: any

}

